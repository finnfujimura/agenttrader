---
phase: 01-parity-safety-and-benchmark-foundations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - agenttrader/core/parity.py
  - agenttrader/core/backtest_engine.py
  - agenttrader/data/backtest_artifacts.py
  - agenttrader/db/schema.py
  - tests/unit/test_phase1_parity_contract.py
  - tests/unit/test_backtest_progress.py
autonomous: true
requirements:
  - PARI-01
  - PARI-02
  - PARI-03
  - PARI-04
  - PARI-05
user_setup: []
must_haves:
  truths:
    - "A parity-enabled backtest run returns deterministic pass/fail status on identical baseline and candidate inputs."
    - "Parity comparison includes markets_tested, final_value, full equity_curve, and full trades with strict equality."
    - "Parity failure payload includes first_mismatch_path, mismatched field excerpts, and per-field mismatch counts in machine-readable JSON."
  artifacts:
    - path: "agenttrader/core/parity.py"
      provides: "Canonical serialization, hashing, and strict diff logic for parity runs."
      contains: "def compare_backtest_results("
    - path: "agenttrader/core/backtest_engine.py"
      provides: "Parity execution orchestration and parity status propagation in run results."
      contains: "parity"
    - path: "tests/unit/test_phase1_parity_contract.py"
      provides: "Unit coverage for strict equality checks and mismatch diagnostics."
      contains: "test_parity_"
  key_links:
    - from: "agenttrader/core/backtest_engine.py"
      to: "agenttrader/core/parity.py"
      via: "Engine invokes canonical comparator and hash generator before finalizing result payload."
      pattern: "compare_backtest_results"
    - from: "agenttrader/core/backtest_engine.py"
      to: "agenttrader/data/backtest_artifacts.py"
      via: "Engine persists parity diff artifacts and writes artifact references into run payloads."
      pattern: "parity_artifact"
---

<objective>
Implement deterministic parity comparison infrastructure for baseline vs candidate backtest runs.

Purpose: Make parity a strict correctness gate before any migration slice can be considered valid.
Output: Canonical comparator/hashing utilities, backtest-engine parity wiring, and machine-readable mismatch artifacts with tests.
</objective>

<execution_context>
@C:/Users/finnf/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/finnf/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-CONTEXT.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-RESEARCH.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-VALIDATION.md
@agenttrader/core/backtest_engine.py
@agenttrader/data/backtest_artifacts.py
@agenttrader/db/schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical parity compare and deterministic hashing module</name>
  <files>agenttrader/core/parity.py, tests/unit/test_phase1_parity_contract.py</files>
  <action>Create parity helpers that canonicalize comparison payloads, compute deterministic SHA-256 hashes, and produce first-mismatch diagnostics for nested structures. Cover strict equality for markets_tested, final_value, full equity_curve, and full trades. Include mismatch metadata with JSON-safe values and stable field-path pointers.</action>
  <verify>
    <automated>rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\unit\test_phase1_parity_contract.py -x</automated>
  </verify>
  <done>Comparator module exists with deterministic output; tests prove pass, hash mismatch, and first-mismatch reporting behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire parity gate into BacktestEngine run lifecycle</name>
  <files>agenttrader/core/backtest_engine.py, tests/unit/test_phase1_parity_contract.py, tests/unit/test_backtest_progress.py</files>
  <action>Integrate parity execution mode into BacktestEngine so baseline and candidate paths can be compared on identical normalized inputs. Ensure run payloads include parity.enabled, parity.status, baseline/candidate hash values, and mismatch summary. Treat parity failure as run failure for parity-enabled runs while preserving deterministic diagnostics.</action>
  <verify>
    <automated>rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\unit\test_phase1_parity_contract.py tests\unit\test_backtest_progress.py -x</automated>
  </verify>
  <done>Engine emits strict parity pass/fail status with required fields, and parity failure produces failure-state run metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Persist parity artifacts and enforce retention policy</name>
  <files>agenttrader/data/backtest_artifacts.py, agenttrader/db/schema.py, agenttrader/core/backtest_engine.py, tests/unit/test_phase1_parity_contract.py</files>
  <action>Add artifact helpers for parity diagnostics and deterministic naming. Persist compact parity summaries in results JSON and write large diff payloads to artifact storage. Implement retention policy that always keeps failures and keeps only a bounded rolling window for passing parity artifacts.</action>
  <verify>
    <automated>rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\unit\test_phase1_parity_contract.py -x</automated>
  </verify>
  <done>Parity summary and artifact references are persisted consistently, and retention behavior is covered by unit tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\unit\test_phase1_parity_contract.py -x`
- [ ] `rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\unit\test_backtest_progress.py -x`
- [ ] Parity failure payload includes `first_mismatch_path` and mismatch counts in JSON-safe form.
</verification>

<success_criteria>

- All tasks completed
- PARI-01 through PARI-05 are implemented with strict deterministic parity behavior
- Parity diagnostics are machine-readable and include first mismatch location
- Artifact retention policy keeps all failures and bounded passing history
</success_criteria>

<output>
After completion, create `.planning/phases/01-parity-safety-and-benchmark-foundations/01-01-SUMMARY.md`
</output>
