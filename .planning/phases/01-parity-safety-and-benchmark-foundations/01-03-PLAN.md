---
phase: 01-parity-safety-and-benchmark-foundations
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - agenttrader/core/benchmark_gate.py
  - agenttrader/core/backtest_engine.py
  - agenttrader/cli/experiments.py
  - agenttrader/benchmarks/phase1_suite.yaml
  - tests/integration/test_phase1_benchmark_gates.py
  - tests/fixtures/phase1_backtest_inputs/README.md
autonomous: true
requirements:
  - BENC-01
  - BENC-02
user_setup: []
must_haves:
  truths:
    - "Benchmark runs use a representative, versioned suite that includes broad-subscription full-span and mixed execution-mode workloads."
    - "Each migration slice is evaluated against explicit threshold policy (speed target and allowed parity failures) with deterministic pass/fail output."
    - "Benchmark acceptance fails automatically when parity status is not pass, even if runtime is faster."
  artifacts:
    - path: "agenttrader/benchmarks/phase1_suite.yaml"
      provides: "Pinned benchmark dataset definitions, run parameters, and per-slice thresholds."
      contains: "thresholds"
    - path: "agenttrader/core/benchmark_gate.py"
      provides: "Benchmark protocol execution and threshold evaluation with parity prerequisite checks."
      contains: "def evaluate_benchmark_gate("
    - path: "tests/integration/test_phase1_benchmark_gates.py"
      provides: "Integration coverage for benchmark suite execution, threshold checks, and parity prerequisite behavior."
      contains: "test_benchmark_gate"
  key_links:
    - from: "agenttrader/cli/experiments.py"
      to: "agenttrader/core/benchmark_gate.py"
      via: "CLI benchmark entrypoint loads suite profile and invokes gate evaluator."
      pattern: "benchmark_gate"
    - from: "agenttrader/core/benchmark_gate.py"
      to: "agenttrader/core/backtest_engine.py"
      via: "Gate execution consumes backtest run parity status before applying speed/variance thresholds."
      pattern: "parity"
---

<objective>
Define and execute representative benchmark gates with explicit acceptance thresholds and parity prerequisites.

Purpose: Prevent performance claims from bypassing correctness by enforcing benchmark policy tied to parity outcomes.
Output: Versioned benchmark suite manifest, benchmark gate evaluator, CLI entrypoint wiring, and integration tests.
</objective>

<execution_context>
@C:/Users/finnf/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/finnf/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-CONTEXT.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-RESEARCH.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-VALIDATION.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-01-PLAN.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-02-PLAN.md
@agenttrader/cli/experiments.py
@agenttrader/core/backtest_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add versioned benchmark suite manifest and fixture index</name>
  <files>agenttrader/benchmarks/phase1_suite.yaml, tests/fixtures/phase1_backtest_inputs/README.md, tests/integration/test_phase1_benchmark_gates.py</files>
  <action>Create a benchmark suite manifest covering small/medium/large representative workloads, including broad-subscription full-span and mixed execution-mode cases. Define per-slice threshold fields for minimum speedup, maximum parity failures, run count, and variance allowance. Add fixture documentation that pins dataset IDs and expected profile usage.</action>
  <verify>
    <automated>rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\integration\test_phase1_benchmark_gates.py -k manifest -x</automated>
  </verify>
  <done>Benchmark suite manifest is versioned, parseable, and covered by integration tests for schema and required scenario presence.</done>
</task>

<task type="auto">
  <name>Task 2: Implement benchmark gate evaluator with parity prerequisite enforcement</name>
  <files>agenttrader/core/benchmark_gate.py, agenttrader/core/backtest_engine.py, tests/integration/test_phase1_benchmark_gates.py</files>
  <action>Implement benchmark gate logic that runs warmups, collects repeated measured runs, computes median runtime, and evaluates thresholds per slice. Enforce parity as a hard prerequisite: if parity status is not pass for a measured run, gate status must fail with explicit reason before performance scoring. Return machine-readable gate summaries for downstream CLI/MCP consumption.</action>
  <verify>
    <automated>rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\integration\test_phase1_benchmark_gates.py -k gate -x</automated>
  </verify>
  <done>Gate evaluator returns deterministic pass/fail outputs with explicit failure reasons and parity-prerequisite behavior verified.</done>
</task>

<task type="auto">
  <name>Task 3: Wire benchmark gate execution into CLI experiments command</name>
  <files>agenttrader/cli/experiments.py, agenttrader/core/benchmark_gate.py, tests/integration/test_phase1_benchmark_gates.py</files>
  <action>Add or extend a CLI experiments workflow that loads the suite manifest, executes benchmark cases, and prints/stores machine-readable threshold results. Include per-slice summary output with median runtime, threshold deltas, parity gate status, and final acceptance verdict. Preserve existing CLI behavior for unrelated experiment commands.</action>
  <verify>
    <automated>rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\integration\test_phase1_benchmark_gates.py -x</automated>
  </verify>
  <done>CLI can run the representative benchmark suite end-to-end and emits explicit acceptance decisions keyed by slice thresholds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\integration\test_phase1_benchmark_gates.py -x`
- [ ] Benchmark manifest includes broad-subscription full-span and mixed execution-mode workload definitions.
- [ ] Benchmark gate result fails when parity does not pass, regardless of performance metrics.
</verification>

<success_criteria>

- All tasks completed
- BENC-01 and BENC-02 are implemented with executable benchmark policy and thresholds
- Representative suite and threshold policy are versioned and reproducible
- Benchmark acceptance remains blocked by parity failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-parity-safety-and-benchmark-foundations/01-03-SUMMARY.md`
</output>
