---
phase: 01-parity-safety-and-benchmark-foundations
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - agenttrader/core/backtest_engine.py
  - agenttrader/data/models.py
  - agenttrader/cli/backtest.py
  - agenttrader/cli/main.py
  - agenttrader/mcp/server.py
  - tests/unit/test_phase1_rollout_flags.py
  - tests/unit/test_mcp_source_routing.py
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-04
user_setup: []
must_haves:
  truths:
    - "Operators can independently toggle migration slices for executor, marshalling, and fill behavior at runtime."
    - "A global runtime override can force legacy Python execution without code changes."
    - "Enabled slices default to guarded shadow mode, and any parity failure or Rust runtime error triggers automatic fallback to legacy."
    - "CLI and MCP backtest responses expose active execution path, fallback state, and effective flag values."
  artifacts:
    - path: "agenttrader/core/backtest_engine.py"
      provides: "Central execution-path resolver that applies per-slice flags and global legacy override."
      contains: "force_legacy"
    - path: "agenttrader/cli/backtest.py"
      provides: "CLI flag inputs and output payload fields for active path transparency."
      contains: "--force-legacy"
    - path: "agenttrader/mcp/server.py"
      provides: "MCP tool schema/handler support for slice flags and active path reporting."
      contains: "active_path"
    - path: "tests/unit/test_phase1_rollout_flags.py"
      provides: "Unit coverage for independent slice flags and global override behavior."
      contains: "test_"
  key_links:
    - from: "agenttrader/cli/backtest.py"
      to: "agenttrader/core/backtest_engine.py"
      via: "CLI argument parser maps slice controls and force-legacy switch into engine config."
      pattern: "force_legacy|slice"
    - from: "agenttrader/mcp/server.py"
      to: "agenttrader/core/backtest_engine.py"
      via: "MCP run_backtest and validate_and_backtest pass execution-control fields into engine calls."
      pattern: "run_backtest|validate_and_backtest"
---

<objective>
Add operational safety controls and runtime transparency for migration slices across CLI and MCP.

Purpose: Ensure operators can rollback instantly and verify which execution path was active in every run.
Output: Unified runtime control surface (per-slice flags + global legacy switch), plus CLI/MCP visibility of effective flags and active path.
</objective>

<execution_context>
@C:/Users/finnf/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/finnf/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-CONTEXT.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-RESEARCH.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-VALIDATION.md
@.planning/phases/01-parity-safety-and-benchmark-foundations/01-01-PLAN.md
@agenttrader/core/backtest_engine.py
@agenttrader/cli/backtest.py
@agenttrader/mcp/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime flag model, guarded shadow-mode defaults, and path-resolution policy in engine config</name>
  <files>agenttrader/core/backtest_engine.py, agenttrader/data/models.py, tests/unit/test_phase1_rollout_flags.py</files>
  <action>Extend runtime configuration to represent per-slice flags and a global force-legacy override. Implement a single path-resolution function that enforces: force-legacy wins, enabled slices default to guarded shadow mode (legacy remains authoritative until promotion), disabled slices route to legacy for that slice, and any parity failure or Rust runtime error automatically falls back to legacy with explicit fallback metadata. Emit deterministic active-path/shadow/fallback status fields for strict vs synthetic execution modes.</action>
  <verify>
    <automated>rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\unit\test_phase1_rollout_flags.py -x</automated>
  </verify>
  <done>Engine resolves effective execution path deterministically from flags, with guarded shadow-mode default and automatic fallback on parity failure/runtime errors covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Expose rollout controls and active-path fields in CLI backtest UX</name>
  <files>agenttrader/cli/backtest.py, agenttrader/cli/main.py, tests/unit/test_phase1_rollout_flags.py</files>
  <action>Add CLI arguments for per-slice controls and force-legacy runtime override, wire them into backtest invocation, and include effective flags plus active path fields in JSON output and human summary output. Surface guarded shadow-mode and fallback status fields (including reason/trigger) so parity-failure and runtime-error fallback behavior is externally visible. Keep defaults backward-compatible and avoid breaking existing commands.</action>
  <verify>
    <automated>rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\unit\test_phase1_rollout_flags.py -x</automated>
  </verify>
  <done>CLI accepts new control flags, executes with expected behavior, and prints active path, shadow-mode state, and fallback fields consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Extend MCP backtest schemas and payload transparency for runtime controls</name>
  <files>agenttrader/mcp/server.py, tests/unit/test_phase1_rollout_flags.py, tests/unit/test_mcp_source_routing.py</files>
  <action>Update MCP `run_backtest` and `validate_and_backtest` schemas to accept rollout flags and force-legacy override. Ensure response payloads include active path, guarded shadow-mode state, fallback reason/trigger (if any), and effective controls. Add tests confirming MCP requests propagate controls to the engine, auto-fallback on parity/runtime failures, and expose transparent status fields in result payloads.</action>
  <verify>
    <automated>rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\unit\test_phase1_rollout_flags.py tests\unit\test_mcp_source_routing.py -x</automated>
  </verify>
  <done>MCP tools accept and report execution controls with deterministic payload shape, and automated tests validate shadow-mode defaults plus parity/runtime-error fallback status visibility.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\unit\test_phase1_rollout_flags.py -x`
- [ ] `rtk venv\Scripts\python.exe -m pytest --basetemp=codex_tmp_test tests\unit\test_mcp_source_routing.py -x`
- [ ] Automated tests verify guarded shadow-mode default posture for enabled slices and automatic legacy fallback on parity failure or Rust runtime error.
- [ ] CLI and MCP result payloads expose `active_path`, `effective_flags`, `shadow_mode`, and fallback status fields.
</verification>

<success_criteria>

- All tasks completed
- SAFE-01, SAFE-02, and SAFE-04 are implemented with explicit runtime controls
- Global force-legacy behavior is available at runtime for both CLI and MCP
- Operators can observe active execution path and flags without inspecting code
</success_criteria>

<output>
After completion, create `.planning/phases/01-parity-safety-and-benchmark-foundations/01-02-SUMMARY.md`
</output>
